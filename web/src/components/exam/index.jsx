import styles from './exam.module.scss';

function Exam() {
    return (
        <div className={`${styles.examStyle}`}>
            This is Exam Component with SCSS
        </div>
    )
}

export default Exam